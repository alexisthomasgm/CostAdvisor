# Copy to .env.local for local dev overrides (optional — empty baseURL works with Vite proxy).
# In production, set VITE_API_BASE_URL in Cloudflare Pages' environment variables UI.

# Backend API base URL. Leave blank in dev to use the Vite proxy.
VITE_API_BASE_URL=
